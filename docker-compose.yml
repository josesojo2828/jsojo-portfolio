services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_AUTH_URL=https://quanticarch.com
    container_name: jsojo_website
    restart: always
    ports:
      - "9080:3000"
    networks:
      - dgobyke_net

networks:
  dgobyke_net:
    driver: bridge
